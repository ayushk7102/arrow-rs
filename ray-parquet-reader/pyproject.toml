[build-system]
build-backend = "maturin"
requires = ["maturin>=1.0,<2.0"]

[project]
name = "ray-parquet-rs"
requires-python = ">=3.10,<4"
dynamic = ["version"]

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "ray_parquet_rs"
python-source = "python"
